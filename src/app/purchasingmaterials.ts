export interface Purchasingmaterials {
    processCode: string,
    process: string,
    partType: string,
    groupPart: string,
    shift: string,
    machine: string,
    date: string,
    quantity: number,
    target: number,
    accum: number,
    targetAccum: number
}
